global:
  smtp_smarthost: 'smtphost:587'   # SMTP server and port
  smtp_from: 'alert@domain.com'   # From email
  smtp_auth_username: 'username'
  smtp_auth_password: 'password'

route:
  receiver: 'team-email'

receivers:
  - name: 'team-email'
    email_configs:
      - to: 'danish.ahmed@domain.com'
        send_resolved: true   # optional: send alert cleared notifications
